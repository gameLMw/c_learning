#include <stdio.h>
#include <ctype.h>

int main() {
    char first, second;
    
    printf("?????: ");
    
    // ???????
    first = getchar();
    
    // ???????????
    second = getchar();
    
    if (second == '\n' || second == EOF) {
        // ?????????
        printf("????????: %c\n", first);
        // ???????
        program_one(first);
    } else {
        // ???????????
        printf("????????,????: %c,????: %c\n", first, second);
        // ???????
        program_two(first, second);
        
        // ??????(???)
        while (getchar() != '\n');
    }
    
    return 0;
}

void program_one(char c) {
    printf("???????,????: %c\n", c);
    // ?????????
}

void program_two(char c1, char c2) {
    printf("???????,????: %c ? %c\n", c1, c2);
}
